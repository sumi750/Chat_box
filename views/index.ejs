<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>All Chats!</h3>
    <form action="/chats/new" method="get">
        <button style="cursor: pointer;;">New Chat</button>
    </form>
    <br>
    <br>
    <% for(let chat of chats) { %>
        <div class="chat">
            <p>From : <i><%=chat.from %></i> </p>
            <div class="msg">
                <p> Message : <%=chat.msg %></p>
                <form action="/chats/<%= chat.id%>/edit" method="get">
                    <button style="border-radius: 5px;">Edit</button></form> <br>
                <form action="/chats/<%= chat.id%>?_method=DELETE" method="POST">
                    <button style="border-radius: 5px;" onclick="ConfirmDelete()">Delete</button></form>
            </div>
            <p>  Recevied by :<i>  <%=chat.to %> </i> </p>
            <hr>
            <p><%=chat.created_at.toString().split(" ")[4] %></p>
            <p><%=chat.created_at.toString().split(" ").slice(0,4).join(" ") %></p>
        </div>
        <br><br>
    <% } %>
    <!-- <button>Crate new Chat</button> -->
</body>
</html>